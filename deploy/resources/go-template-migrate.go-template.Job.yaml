apiVersion: batch/v1
kind: Job
metadata:
  name: go-template-migrate
  namespace: go-template
  labels:
    app.kubernetes.io/name: go-template
    app.kubernetes.io/component: migration
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: go-template
        app.kubernetes.io/component: migration
    spec:
      restartPolicy: OnFailure
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: docker.io/fr33dman/go-template:latest
          imagePullPolicy: IfNotPresent
          command:
            - /app/migrate
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
          env:
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  name: go-template-postgres
                  key: host
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  name: go-template-postgres
                  key: port
            - name: POSTGRES_DATABASE
              valueFrom:
                secretKeyRef:
                  name: go-template-postgres
                  key: database
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: go-template-postgres
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: go-template-postgres
                  key: password
            - name: GOBOX_GOOSE_TABLE_NAME
              value: goose_db_version_outbox
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
